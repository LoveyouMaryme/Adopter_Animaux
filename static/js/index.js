changePetElement = document.getElementsByClassName("pet-switch");
getPetCards = document.getElementsByClassName("pet-card");

parentContainerPetCards = leftChangeElement = changePetElement[0];
rightChangeElement = changePetElement[1];

console.log(leftChangeElement);

leftChangeElement.addEventListener("click", () => {
  carousel;
});
